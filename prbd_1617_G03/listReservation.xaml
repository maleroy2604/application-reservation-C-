<f:UserControlBase  x:Class="prbd_1617_G03.listReservation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:f="clr-namespace:PRBD_Framework;assembly=PRBD_Framework"
        xmlns:local="clr-namespace:prbd_1617_G03.Properties"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" 
        mc:Ignorable="d"
        Height="300" Width="300">
    <StackPanel Margin="10">
        <TextBlock FontSize="20" FontWeight="SemiBold" Margin="0,0,0,6">Reserved Clients</TextBlock>
        <TextBlock Text="Identify attendees quickly and open a complete reservation profile in one double-click." Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,10"/>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <TextBox Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}" Width="220" ToolTip="Search by first or last name"/>
            <Button Content="Clear" Command="{Binding ClearFilterCommand}" Margin="8,0,0,0"/>
        </StackPanel>
        <ListView x:Name="ListView" ItemsSource="{Binding Clients}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding ResDisplayCommand}"
                                        CommandParameter="{Binding ElementName=ListView, Path=SelectedItem}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel >
                        <TextBlock FontSize="14" FontWeight="SemiBold" Width="120" TextAlignment="Left" Text="{Binding clientFName}" />
                        <TextBlock FontSize="14" FontWeight="SemiBold" Width="120" TextAlignment="Left" Text="{Binding clientLName}" />
                        
                        
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Create Reservation" Command="{Binding NewReservationCommand}" Margin="0,10,0,0" HorizontalAlignment="Left" Visibility="{Binding VendorVisible}" />
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Margin="0,8,0,0" Foreground="#666" Text="Tip: Double-click a client to view reservation details and update seats."/>
    </StackPanel>

</f:UserControlBase>
